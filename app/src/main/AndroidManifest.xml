<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotivationAndProductivityApp">
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".weekly_reminder.WeeklyReminderActivity"
            android:exported="false" />

        <receiver
            android:name=".repeating_reminder.NotificationPanelReceiver"
            android:enabled="true" />

        <receiver
            android:name=".weekly_reminder.WeeklyReminderReceiver"
            android:enabled="true" />

        <activity
            android:name=".repeating_reminder.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>